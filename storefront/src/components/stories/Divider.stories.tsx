import type { Meta } from '@storybook/react';
import { Divider } from '../atoms/Divider';

const meta: Meta = { title: 'Atoms/Divider', component: Divider };
export default meta;

export const Default = () => <Divider />;
